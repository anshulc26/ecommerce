<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to t('global.title'), root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if current_user.buyer? %>
          <li class="nav-item">
            <%= link_to t('global.marketplace'), marketplace_path, class: "nav-link #{active_class(marketplace_path)}" %>
          </li>
        <% elsif current_user.seller? %>
          <li class="nav-item">
            <%= link_to t('products.heading'), products_path, class: "nav-link #{active_class(products_path)}" %>
          </li>
        <% end %>
      </ul>
    </div>
    <%= link_to t('global.sign_out'), destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm pull-end" %>
  </div>
</nav>
